name: "swap"

version: "0.0.0"
main: leafor.swap.Main
api-version: 1.16
depend:
  - NoteBlockAPI
  - "Multiverse-Core"
permissions:
  leafor.swap.forceStart:
    description: Force start game
    default: op
commands:
  force_start:
    description: Force start Game
    usage: /<command>
    permission: leafor.swap.forceStart
